<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.javatpoint.com.apppedidosmovilmicrosip">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.DetallePedidosActivity"></activity>
        <activity android:name=".Activities.PedidosRecyclerviewActivity" />
        <activity android:name=".Activities.ArticulosRecyclerviewActivity" />
        <activity android:name=".Activities.InformacionClienteActivity" />
        <activity android:name=".Activities.ClientRecyclerViewActivity" />
        <activity android:name=".Activities.MainMenuActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>